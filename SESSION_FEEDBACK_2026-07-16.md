# 2026-07-16 不具合報告・要望（記録のみ、対応・調査未着手）

ユーザーからの明示的な指示により、分析・対応方針の検討は行わず、報告内容をそのまま記録する。

## 不具合報告・仕様確認

| # | 内容 |
|---|------|
| 1 | World（世界観）の編集画面から、部屋のつながり（経路／`room_connections`）を設定するための導線がない |
| 2 | どのWorldにも属さない「未分類」の部屋を新規登録する方法がない |
| 3 | CharactersPageで「外見・衣装」タブを表示した状態のまま別のキャラクターへ切り替えると、danbooruタグ等の表示が新しいキャラの内容に更新されない |
| 4 | 表情マスター（ExpressionTypesPage）で表情アイコンの画像を生成する際、その表情に設定した画像生成用タグ（`danbooru_tag`）が生成に反映されない |
| 5 | 設定画面の「使用可能な変数」一覧が最新の変数構成に追従していない。イベントの画像生成系（`${targetN.category}`等のプレースホルダー一覧）についても同様に最新化が必要 |
| 6 | イベントのクールダウン（`cooldown_turns`）・最大発火回数（`max_fires_per_session`）を、部屋移動（セッションをまたぐ）でリセットできるようにしたい。現状は`event_fire_history`に`room_session_id`列が無く、常に`playthrough_id`（ルート単位累計）でのみ判定される固定仕様——`max_fires_per_session`という項目名だがセッション単位ではない。対応するには`event_fire_history`への列追加＋イベント定義側にリセット範囲の選択項目＋`passesCooldownAndMaxFires`の分岐が必要（要実装、今回は記録のみ） |

## 追加要望

| # | 内容 |
|---|------|
| 7 | 曜日・休日の概念が欲しい（土日は休みになる、休日は私服になる、等） |
| 8 | 部屋単位で時間帯ごとの出現キャラ制御が欲しい（夜間に学生キャラが学校内に残っている等、不自然な状態が起きている） |
| 9 | 移動用の欄（部屋間接続のボタン一覧）を折りたためるようにしたい。場所を取りすぎている |
| 10 | チャット機能（メイン機能）のUI全体的な見直しをどこかで行う。v0.2.x系列の主題候補として挙げておく |
