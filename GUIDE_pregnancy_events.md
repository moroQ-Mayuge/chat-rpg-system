# 妊娠イベントの作り方（画面操作の手順）

妊娠の**機構**はアプリに入っているが、それを動かす**イベントは1本も入っていない**。
受胎も発覚も出産も、すべて「イベント定義」画面で自分で作る。

この資料は、実際の画面のどこを触るかだけを書く。演出文（台詞・ナレーション）は
世界観ごとの判断なので空欄にしてある。

作るイベントは全部で3〜4本：

1. **受胎判定** — 「そういうこと」があったときに `受胎判定` を撃つ
2. **妊娠の発覚** — 本人が気づく
3. **出産** — 臨月になったら終わらせる
4. （任意）**流産・中絶** — 別の終わり方

---

## 1. 準備：世界観画面の設定

上部メニュー **「世界観」** → 対象の世界観を選んで編集。

### 「妊娠・出産」セクション

| 画面上の項目 | やること |
|---|---|
| ☑ **このWorldで妊娠を有効にする** | ONにする。OFFのままだと `受胎判定` は何もしない |
| **妊娠期間（ゲーム内日数）** | 数十日にする（後述） |
| **受胎率の倍率** | 通常は 1。0にすると受胎しなくなる |

チェックを入れると、その下に妊娠期間・受胎率の倍率・キャラの加齢・子の登場…が開く。

> **妊娠期間の目安。** 実プレイでは1日進むのに100通以上のやりとりが必要になる。
> 現実準拠の280日にすると出産まで到達しない。遊ぶなら数十日、動作確認なら数日。
> 段階は期間に対する**割合**で決まるので、短くしても進み方の形は変わらない。

### 「妊娠しやすさの周期」セクション（同じ世界観画面・少し上）

| 画面上の項目 | やること |
|---|---|
| ☑ **このWorldで周期を有効にする** | ONにすると、受胎の確率が日によって変わる |
| **周期の長さ（ゲーム内日数）** | 既定28日 |

ONにしなくても妊娠自体は動く（その場合の受胎率は一律20%）。

### 「LLMによる1回あたりの値の変動上限」（同じ世界観画面・下の方）

**必ず入れておくこと。** 空欄だとLLMが返した増減がそのまま通り、
`好感度|+100` が一発で入ってしまう。

---

## 2. 準備：キャラクター画面の設定

上部メニュー **「キャラクター」** → 妊娠しうるキャラを選ぶ。

- ☑ **妊娠しやすさの周期を持たせる** … 世界観側がONのときだけ効く
- **周期のずらし日数** … 既定値はキャラごとに自動で散らしてある。触らなくてよい

---

## 3. 覚えることは「フラグ2つ」だけ

イベントから見るのはこの2つ。どちらも**キャラに付くフラグ**で、日付から自動で
更新される。テーブルを直接見る必要はない。

### `pregnancy_stage`（妊娠中のキャラにだけ立つ）

| 値 | 妊娠期間に対する割合 | 期間21日なら |
|---|---|---|
| `未発覚` | 0〜12% | 1〜3日目 |
| `兆候` | 12〜22% | 4〜5日目 |
| `発覚可能` | 22〜35% | 6〜8日目 |
| `安定期` | 35〜70% | 9〜15日目 |
| `後期` | 70〜92% | 16〜20日目 |
| `臨月` | 92%〜 | 21日目〜 |

- 期間を過ぎても**臨月に留まる**（巻き戻らない）。出産イベントを書き忘れても壊れない
- 妊娠が終わると**フラグごと消える**。「臨月」を条件にしたイベントが出産後も
  発火し続けることはない

### `cycle_phase`（周期を持たせたキャラに立つ）

`安全` / `やや安全` / `やや危険` / `危険` / `最危険` の5段階。排卵日に向けて上がり、
直後に急落する。

**妊娠中は `妊娠中` に上書きされる。**「今日は危険日だから」を妊娠している相手に
言わせないため。妊娠しているかの判定にも使える。

### 更新のタイミング

- **日付が変わったとき**（時間経過・時間跳躍で日を跨いだとき）
- **`受胎判定` / `妊娠の発覚・終了` を実行した直後**

日を跨がない時間帯送りでは変わらない。

---

## 4. イベント画面の共通操作

上部メニュー **「イベント」** → 左の **「+ 新規イベント」**。

右の編集フォームで、どのイベントも以下を触る：

| 欄 | 妊娠まわりでの指定 |
|---|---|
| **イベント名** | 任意（未入力だと保存ボタンが押せない） |
| **スコープ** | `room_template（特定の部屋のみ）` を選ぶ（→ 6章の落とし穴） |
| **対象部屋** | スコープを room_template にすると出る。貼り付ける部屋を選ぶ |
| **条件の結合** | `AND` のまま |
| **クールダウン(ターン)** / **最大発火回数** | 出産・発覚は `最大発火回数` に `1` を入れておくと安全 |

条件は **「発火条件」** セクションの **「+ 条件を追加」**、
アクションは **「アクション」** セクションの **「+ アクションを追加」** で足す。
どちらも行の左端のプルダウンで種類を選び直す。

最後に右下の **「保存」**。

---

## 5. イベントの中身

### 5-1. 受胎判定

**発火条件**（「+ 条件を追加」）

| 種類 | 設定 |
|---|---|
| `キーワード検出` または `LLM判定` | その出来事があったかを判定する。演出に合わせて書く |
| `ステータス所持` / `所持アイテム` | 避妊を弾きたいとき。**「持っていない場合に成立」**にチェック |

> 避妊の機構はアプリ側に無い。道具立ては世界観ごとに違うので、既存の条件で
> イベント側を弾く。

**アクション**（「+ アクションを追加」→ 種類を **`受胎判定`** に変更）

| 欄 | 選ぶもの |
|---|---|
| **対象キャラ** | `@メンション中のキャラ`（相手が固定なら個別のキャラ名でもよい） |
| **@メンション上限人数（空欄=全員）** | 対象キャラを `@メンション中のキャラ` にすると出る。通常は `1` |
| **確率（空欄で周期から自動）** | 空欄推奨 |

**確率を空欄にしたときの値**：

| その日の妊娠しやすさ | 確率 |
|---|---|
| 最危険 | 50% |
| 危険 | 35% |
| やや危険 | 20% |
| やや安全 | 8% |
| 安全 | 2% |

周期が無効なら一律20%。いずれも世界観の**受胎率の倍率**が掛かる。
確率を明示した場合（`1` を入れて必ず妊娠させる、など）は**倍率が掛からない**
——演出として書いた「必ず」が設定で「たぶん」に変わらないようにするため。

**何も起きない場合**（いずれも黙って素通りする）：

- 世界観で妊娠が無効
- ルートがナレーター視点（父になる「あなた」が居ないため）
- 対象がモブキャラ（モブはセッションごとに状態がリセットされるため）
- 対象が既に妊娠中

`受胎判定` は文章を出さない。受けたかどうかをその場で見せたいなら、
**「成功/失敗分岐を有効にする」**を使って結果ごとにアクションを分ける。

---

### 5-2. 妊娠の発覚

**発火条件**

| 種類 | 設定 |
|---|---|
| `フラグ状態` | **フラグキー** `pregnancy_stage` ／ **比較** `==` ／ **値** `発覚可能` ／ **対象キャラ** `同席者の誰か1人でも` ／ **スコープ** `ルート永続` |
| 発覚の契機 | 保健室に居る（部屋スコープで足りる）／検査薬を持っている（`所持アイテム`）／会話の内容（`LLM判定`） |

**アクション**

| 種類 | 設定 |
|---|---|
| `妊娠の発覚・終了` | **対象キャラ** = 妊娠している相手 ／ **操作** `本人が妊娠に気づく` |
| `台詞・ナレーション挿入` | （空欄・演出文を書く） |
| `記憶を追加（ルートに永続）` | （空欄・演出文を書く） |

> **発覚するまで、LLMには妊娠のことを一切渡していない。** 気づく前は体調の変化だけが
> キャラ情報に載る。ここで「本人が妊娠に気づく」を実行して初めて、本人が知っている
> 情報として扱われる。二度実行しても、最初に気づいた日は上書きされない。

---

### 5-3. 出産

**発火条件**

| 種類 | 設定 |
|---|---|
| `フラグ状態` | **フラグキー** `pregnancy_stage` ／ **比較** `==` ／ **値** `臨月` ／ **対象キャラ** `同席者の誰か1人でも` ／ **スコープ** `ルート永続` |

**アクション**

| 種類 | 設定 |
|---|---|
| `妊娠の発覚・終了` | **操作** `妊娠を終了する` ／ **結果** `出産` ／ **子の名前** ／ **子の性別**（例：女児） |
| `台詞・ナレーション挿入` | （空欄・演出文を書く） |

> **出産は自動では起きない。** エンジンはターンでしか動かず、日数の経過だけで何かを
> 発火させる仕組みを持っていない。このイベントを書いて初めて出産する。

---

### 5-4. 流産・中絶（任意）

出産イベントとは**別のイベント**として作る（同じイベントに混ぜない）。

| 欄 | 設定 |
|---|---|
| 発火条件 | 選択や状況に応じて。`フラグ状態` で `pregnancy_stage` が `存在する` ＋ 契機の条件 |
| アクション `妊娠の発覚・終了` | **操作** `妊娠を終了する` ／ **結果** `流産` または `中絶` |
| アクション `記憶を追加（ルートに永続）` | （空欄・演出文を書く） |

---

## 6. 条件「フラグ状態」の書き方まとめ

妊娠まわりで新しい条件タイプは要らない。すべて **`フラグ状態`** で読む。

| 見たいこと | フラグキー | 比較 | 値 |
|---|---|---|---|
| 臨月になった | `pregnancy_stage` | `==` | `臨月` |
| 妊娠している | `pregnancy_stage` | `存在する` | （欄が消える） |
| 妊娠していない | `pregnancy_stage` | `存在しない` | （欄が消える） |
| 今日は危険日 | `cycle_phase` | `==` | `危険` |
| 妊娠中か（周期側から） | `cycle_phase` | `==` | `妊娠中` |

**対象キャラ**は `同席者の誰か1人でも` か `@メンション中のキャラ`、または個別のキャラ名。
`未指定（グローバル）` にすると**キャラのフラグではなくルート全体のフラグ**を見に行くので、
妊娠まわりでは必ずキャラを指定する。

対象キャラを指定すると **スコープ** が出る。妊娠のフラグは `ルート永続`（既定）。

---

## 7. 落とし穴

**イベントは世界観単位ではない。** スコープは `global（全部屋共通）` と
`room_template（特定の部屋のみ）` の2つだけで、World を指定する欄が無い。
`global` にすると**全ての世界観で生きてしまう**。特定の世界観だけで動かしたいなら、
その世界観だけが持つ部屋に `room_template` で貼る。

**出産は自動発火しない。** 臨月を条件にしたイベントが要る（5-3）。

**フラグの更新は日跨ぎ。** 時間帯を送っただけでは段階は変わらない。動作確認では
アクション `時間経過` の **「進める時間帯の数」** を、日を跨ぐぶんだけ入れる。
あるいは `時間跳躍（n日後・n年後）` で日数を直接進める。

**妊娠中は周期の段階が出ない。** `cycle_phase` は `妊娠中` になるので、危険日を
条件にしたイベントは妊娠中のキャラには当たらない。

**子のキャラ化は世界観画面の別設定。** 出産しても、世界観画面の **「子の登場」** が
`登場しない（記録と会話の話題としてのみ存在）` の間は子はキャラにならない。
`早熟（出産から一定日数で登場する）` にすると、**「登場までの日数（出産から）」**が
経過した時点でチャット画面の上部に「〜の子が戻る頃合いになりました。」という
通知とボタンが出て、**押して初めてキャラが作られる**（立ち絵も表情もこれから作る
必要があるので、勝手には始まらない）。
`跳躍時に登場する` は**まだ動かない**（選んでも子は登場しない）。

**子の属性キーは母から継がない（既定）。** 世界観画面の
**「子に必ず付ける属性キー（固定枠）」「ランダムで付く属性キーの候補」**
**「母親の属性キーをランダムに引き継ぐ」** の3系統で決める。継承が既定OFFなのは、
母が「生徒」だと、それを継いだ幼児が教室に出てしまうため。

**動作確認のときは日数を短くする。** 妊娠期間と登場までの日数を数日にすれば、
`時間経過` / `時間跳躍` で一周させられる。チャット画面の **「デバッグ情報」**
（折りたたみ）に、周期の段階と子の登場までの残り日数が出る。

---

## 8. 関連：汎用タイマー

「n日経ってから何かが起きる」を自分で書きたいときは、アクション
**`タイマーを張る（n日後）`** が使える。

| 欄 | 内容 |
|---|---|
| **タイマー名** | 例：揺り籠 |
| **何日後** | 日数 |
| **対象キャラ（空欄でルート全体）** | キャラに付けるか、ルート全体に付けるか |
| **メモ（任意・判定には使いません）** | 覚え書き |

期日が来たかは条件 `フラグ状態` で **フラグキー** に `timer:タイマー名` を入れ、
値が `pending`（まだ）か `due`（来た）かを見る。対象キャラを指定して張ったなら、
条件側でも同じキャラを指定する。

同じ名前・同じ対象に張り直すと期日が更新される（約束を先送りする、など）。

**期日が来たタイマーは取り消すまで `due` のまま**なので、一度きりのイベントでは
アクション **`タイマーを取り消す`** を併せて実行する。

出産後に子が戻ってくるまでの待ち時間も、世界観画面の「子の登場」を使わずにこれで
書ける。手紙が届く・注文品の入荷・怪我の治癒・約束の日も同じ形になる。
