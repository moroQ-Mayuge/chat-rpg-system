# 妊娠イベントの作り方

妊娠の**機構**（migration 0076〜0082）はアプリ側に入っているが、それを動かす
**イベントは1本も入っていない**。何をどう組めば動くかをここにまとめる。

イベントの中身（どんな出来事を発火条件にするか、どんな文章を出すか）は世界観ごとの
判断なので、この資料では条件・アクションの組み立て方だけを扱い、演出文は空欄にしてある。

---

## 1. 前提と有効化

世界観の編集画面 →「妊娠・出産」セクション。

| 設定 | 意味 | 備考 |
|---|---|---|
| このWorldで妊娠を有効にする | 全体のスイッチ | OFFだと `conceive` は何もせず `pregnancy_disabled` を返す |
| 妊娠期間（ゲーム内日数） | 受胎から臨月までの長さ | 段階は期間に対する**割合**で決まるので、21日でも280日でも進み方の形は同じ |
| 受胎率の倍率 | 段階別の基礎確率に掛かる | 0で受胎しなくなる |

**妊娠期間の目安。** 実プレイの計測では6073メッセージで52日しか進まない（およそ120通で
1日）。現実準拠の280日は出産まで到達しないので、遊ぶなら数十日、動作確認なら数日にする。

「妊娠しやすさの周期」も併せてONにしておくと、受胎の確率が日によって変わる。
周期はキャラ単位でも有効化が必要（キャラ編集画面）。

---

## 2. 状態の全体像

イベントから読むのは**フラグ2つ**だけ。どちらも日付から導出された値がフラグに
写されているもので、直接テーブルを見る必要はない。

### `pregnancy_stage`（キャラフラグ・playthroughスコープ）

妊娠中のキャラにだけ立つ。妊娠期間に対する割合で決まる。

| 段階 | 期間比 | 21日設定なら |
|---|---|---|
| 未発覚 | 0〜12% | 1〜3日目 |
| 兆候 | 12〜22% | 4〜5日目 |
| 発覚可能 | 22〜35% | 6〜8日目 |
| 安定期 | 35〜70% | 9〜15日目 |
| 後期 | 70〜92% | 16〜20日目 |
| 臨月 | 92%〜 | 21日目〜 |

期間を過ぎても**臨月に留まる**（巻き戻らない）。出産イベントを書き忘れても壊れない。

妊娠が終わると**フラグごと消える**。「臨月」を条件にしたイベントが出産後も
発火し続けることはない。

### `cycle_phase`（キャラフラグ・playthroughスコープ）

妊娠しやすさの段階。`安全` / `やや安全` / `やや危険` / `危険` / `最危険` の5段階で、
排卵日に向けて上がり直後に急落する非対称な形をしている。

**妊娠中は `妊娠中` に上書きされる。**「今日は危険日」を妊娠している相手に
言わせないため。妊娠しているかの判定にも使える。

### 更新のタイミング

- **日付が変わったとき**（`advance_time` や時間帯送りで日を跨いだとき）
- **`conceive` / `end_pregnancy` の実行直後**

日を跨がない時間帯送りでは変わらない。受胎したその日のうちに
`pregnancy_stage` を条件にしたイベントを発火させられるのは、アクション側が
即座に同期しているため。

---

## 3. アクション仕様

### `conceive`（受胎判定）

| パラメータ | 型 | 説明 |
|---|---|---|
| `character_id` | 数値 / `"all_present"` / `"mentioned"` | 対象 |
| `mentioned_limit` | 数値 | `mentioned` のとき、メンション順に何人までか |
| `chance` | 0〜1 | 省略すると周期から自動 |

`chance` を省いたときの確率：

| 妊娠しやすさ | 確率 | 周期が無効な場合 |
|---|---|---|
| 最危険 | 50% | 20%（固定） |
| 危険 | 35% | |
| やや危険 | 20% | |
| やや安全 | 8% | |
| 安全 | 2% | |

いずれも世界観の**受胎率の倍率**が掛かる。`chance` を明示した場合は**倍率が掛からない**
（演出として書いた「必ず」が設定で「たぶん」に変わらないようにするため）。

**何も起きない条件**（いずれも黙って素通りする）：

- 世界観で妊娠が無効 → `pregnancy_disabled`
- ルートがナレーター視点（`protagonist_mode = narrator`）→ `narrator_mode`
  （父になる「あなた」が居ないため）
- 対象がモブキャラ（モブはセッションごとに状態がリセットされるため）
- 対象が既に妊娠中（二重に妊娠しない）

**避妊はここでは扱わない。**道具立ては世界観ごとに違うので、`has_status` や
`has_item` などの既存の条件でイベント側を弾く。

### `end_pregnancy`（発覚・終了）

| パラメータ | 型 | 説明 |
|---|---|---|
| `character_id` | 数値 / `"all_present"` / `"mentioned"` | 対象 |
| `operation` | `"reveal"` / `"end"` | 既定は `end` |
| `outcome` | `"出産"` / `"流産"` / `"中絶"` | `end` のとき。既定は `出産` |
| `child_name` / `child_gender` | 文字列 | `end` かつ `出産` のとき |

`reveal` は**本人が妊娠に気づく**操作。妊娠自体は続く。二度撃っても最初に気づいた日は
上書きされない。

妊娠していない相手に撃っても何も起きない。

---

## 4. 条件の書き方

**新しい条件タイプは要らない。**既存の「フラグ状態」（`flag_state`）で読む。

- フラグキー … `pregnancy_stage` または `cycle_phase`
- 対象キャラ … `mentioned`（@メンション中）／`any_present`（同席者の誰か）／個別指定
- 比較 … `==` / `!=` / `exists` / `not_exists`

例：

| 見たいこと | 条件 |
|---|---|
| 臨月になった | `flag_state(pregnancy_stage, ==, 臨月, character_id: any_present)` |
| 妊娠しているか | `flag_state(pregnancy_stage, exists, character_id: mentioned)` |
| 妊娠していないか | `flag_state(pregnancy_stage, not_exists, character_id: mentioned)` |
| 今日は危険日か | `flag_state(cycle_phase, ==, 危険, character_id: mentioned)` |
| 妊娠中か（周期側から） | `flag_state(cycle_phase, ==, 妊娠中, character_id: mentioned)` |

---

## 5. 組み立て例（骨組み）

演出文は空欄。世界観に合わせて埋める。

### 受胎判定

| 項目 | 内容 |
|---|---|
| スコープ | `room_template`（後述の落とし穴を参照） |
| 条件 | その出来事があったかを判定するもの（`keyword` または `llm_judge`）<br>＋避妊を弾く条件（`has_status` / `has_item` など） |
| アクション | `conceive` … `{ character_id: "mentioned" }` |

`conceive` は成否を黙って返すだけで文章を出さない。受胎したかどうかを
その場で見せたいなら、結果分岐を使って別イベントに繋ぐ。

### 妊娠の発覚

| 項目 | 内容 |
|---|---|
| 条件 | `flag_state(pregnancy_stage, ==, 発覚可能, character_id: any_present)`<br>＋発覚の契機（保健室に居る／検査薬を持っている／会話の内容） |
| アクション | `end_pregnancy` … `{ character_id: ..., operation: "reveal" }`<br>`insert_dialogue` … （空欄）<br>`add_character_memory` … （空欄） |

**発覚するまで、LLMには妊娠のことを一切渡していない。**気づく前は体調の変化だけが
キャラ情報に載る。ここで `reveal` して初めて、本人が知っている情報として扱われる。

### 出産

| 項目 | 内容 |
|---|---|
| 条件 | `flag_state(pregnancy_stage, ==, 臨月, character_id: any_present)` |
| アクション | `end_pregnancy` … `{ character_id: ..., operation: "end", outcome: "出産", child_name: ..., child_gender: ... }`<br>`insert_dialogue` … （空欄） |

**出産は自動では起きない。**エンジンはターンでしか動かず、日数の経過だけで
何かを発火させる仕組みは持っていない。臨月を条件にしたこのイベントを書いて初めて
出産する。

### 終了（流産・中絶）

| 項目 | 内容 |
|---|---|
| 条件 | 選択や状況に応じて（出産イベントとは独立させる） |
| アクション | `end_pregnancy` … `{ operation: "end", outcome: "流産" or "中絶" }`<br>`add_character_memory` … （空欄） |

---

## 6. 落とし穴

**イベントは世界観単位ではない。** `scope` は `global` と `room_template` の2つだけで、
World列が無い。`global` にすると**全ての世界観で生きる**。特定の世界観だけで動かしたい
なら、その世界観だけが持つ部屋に `room_template` スコープで貼る。

**出産は自動発火しない。** 上記のとおり、臨月を条件にしたイベントが必要。

**フラグの更新は日跨ぎ。** 時間帯を送っただけでは段階は変わらない。テストするときは
`advance_time` で時間帯数ぶん送って日を跨がせる。

**関係値の変動上限を設定しておく。** 未設定だと、LLMが返した増減がそのまま入る。
実際に `好感度|+100` が一発で通る状態だった。世界観設定の「LLMによる1回あたりの値の
変動上限」を入れておく。

**妊娠中は周期の段階が出ない。** `cycle_phase` は `妊娠中` になる。危険日を条件にした
イベントは妊娠中のキャラには当たらない。

**子のキャラ化は別の設定。** 出産しても、世界観の「子の登場」が `登場しない` の間は
子はキャラにならない。`早熟` にすると成育日数の経過後にチャット画面へ通知が出て、
ボタンで確定させると初めてキャラが作られる。子に付く属性キーも世界観設定で決める
（母のキーは引き継がない。母が「生徒」だと幼児が教室に出てしまうため）。

**動作確認のときは日数を短くする。** 妊娠期間と成育日数を数日にすれば、
`advance_time` で一周させられる。

---

## 7. 関連：汎用タイマー

「n日経ってから何かが起きる」を自分で書きたいときは、アクション
**「タイマーを張る（n日後）」**（`set_timer`）が使える。期日が来たかは
`flag_state` で `timer:<タイマー名>` を `pending` / `due` として読む。

出産後に子が戻ってくるまでの待ち時間も、世界観設定の「子の登場」を使わずに
これで書ける。手紙が届く・注文品の入荷・怪我の治癒・約束の日も同じ形になる。

**期日が来たタイマーは取り消すまで `due` のまま**なので、一度きりのイベントでは
「タイマーを取り消す」（`clear_timer`）を併せて実行する。
