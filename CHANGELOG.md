# 変更履歴

## v0.1.3

実プレイ継続フェーズでの機能拡充・不具合修正。管理UIの一覧画面統一、部屋参加者制御の作り直し（行単位ランダム＋モブ複製）、モブキャラ概念、通貨・買い物、曜日/祝日カレンダー、LLM主導のステータス自動増減、キャラ-World明示アタッチなど

**管理一覧画面のサイドバー統一 + モバイル対応**
- `RoomTemplatesPage.jsx`独自だった折りたたみWorldグループ実装を`GroupedList.jsx`＋`groupByKeys`（0/1/複数グループ所属レコードに対応）として汎用化し、Characters/Events/Itemsの4画面すべてに同じサイドバー＋編集ペイン構成を適用。あわせてモバイル用スライドアウト一覧ペイン（`useMobileListToggle.js`）も新設
- 画面ごとにグルーピング軸を再検討：キャラは「所属World（推定）」／「属性キー」切替、イベントはWorld（`room_template_id`経由の間接所属、`scope:'global'`は専用バケット）、アイテムはWorld（既存の共通＋World別パターン）
- `ItemsPage.jsx`に選択/編集ペインの概念がなかった（常時表示の新規作成フォームのみ）ため、CharactersPageと同じ`selectedId`/`isNew`パターンで作り直し、既存だった更新APIを初めて配線

**脱衣状態の4トラック独立化 + 衣装タグ抑制 + World4コンテンツ拡充**
- 単一トラックだった`undress_state`排他グループを4本（上半身衣服／上半身下着／下半身衣服／下半身下着）に再設計し、同時に任意の組み合わせを取れるように。共通ロジックは新規`undressState.js`に集約
- `character_statuses.suppresses_outfit_fields`を追加し、`resolveOutfitTags`が状態に応じて特定の衣装タグ列を除外できるように（`generate_image`の`${targetN.category}`・取りこぼし防止タグにも反映）
- `CharacterStatusesPage.jsx`に編集機能を追加。World4（現代学園ファンタジー）向けにステータス14件・行動コマンド10件・イベント10件を作成
- 副産物: `ChatPage.jsx`のキーワードボタンが入力欄の下書き（@メンション含む）を送信前に破棄していたバグを修正

**行動コマンドの3階層カテゴリ化 + ステータス連動表示**
- `action_commands`に`category`/`subcategory`/`sub_subcategory`（自由記述）と`visible_when_status_ids`を追加。チャット欄のコマンドバーがカテゴリ／サブカテゴリのピル切替式に。ステータス条件を満たすまでコマンド自体を非表示にできる

**イベント発火のセッション単位リセット + 前提連鎖のスコープ分離**
- `event_fire_history`に`room_session_id`を追加し、`cooldown_turns`/`max_fires_per_session`をルート全体累計だけでなく部屋滞在（セッション）単位でリセット可能に。前提イベント判定自体のリセット範囲（`prerequisite_reset_scope`）も、要求元イベント自身の設定として独立に選べるように分離

**曜日・祝日カレンダー**
- 季節／時間帯／天候と同じ設計で、World単位の`day_of_week_labels`（週の長さも自由）と`holiday_weekday_indices`（曜日休日）、個別の特定日休日用`world_calendar_holidays`テーブルを追加。既存の`flag_state`条件だけで曜日・祝日限定イベントを組めるように

**@メンション順序が壊れるバグの修正（2箇所）**
- `resolveMentions()`が`session.participants`（固定順）を`filter`していたため、メッセージ中の実際の`@名前`出現順ではなく参加者一覧側の順序になっていた不具合を修正——本文中の出現位置でソートするように変更
- 後日、同根の不具合がもう1段下流の3箇所（`generateImage.js`/`insertDialogue.js`/`setSceneSituation.js`の`candidateParticipants`構築）にも残っていたことが判明し、あわせて修正。`${target1}`が先頭@メンションのキャラに正しく解決されるようになった

**部屋スロットの複数キャラ＋時間帯制御、キャラ状態のWorld共有化**
- `world_room_slot_assignments`が1枠に複数キャラ・キャラごとの時間帯制限を持てるように。属性キー一致による自動出現も部屋入室時に評価
- `character_statuses`をWorld専属の単一`world_id`列から`world_character_statuses`中間テーブル経由の共有マスタへ変更し、複数Worldでの使い回しにステータス複製が不要に
- 参照アンカーキャンバスに3pxの区切り線（2koma風構図の誘導）、設定画面のテスト生成にフルキャンバスプレビューを追加

**モブキャラ概念 + 家族/モブコンテンツ**
- `characters.is_mob`フラグを追加。モブキャラの関係値・自己ステータス・呼び方は`playthrough_id`でなく`room_session_id`スコープになり、部屋セッションごとにリセットされる（同じcharacter_idが複数セッションで別人を表現できるように）
- World4向けに固定キャラ3体（妹・友人）＋モブ5種（小中高生・教師・大人）を追加

**参加者ランダム割り当ての行単位再設計 + モブ複製 + `すべて`ワイルドカードタグ**
- 枠単位のランダムトグルをまず追加した直後、同日中に行単位（`character_id IS NULL`の行＝ランダム枠）方式へ全面置き換え。1枠に複数のランダム行を作ることで0〜N人の変動を表現でき、`random_fill_mode`（`always`/`probability`）を行ごとに設定可能に
- モブキャラ（`is_mob`）のみ複数のランダム行から重複選出を許可——`room_session_characters`の主キーを複合PKからsurrogate `id` PKへ変更し、`participantNaming.js`が英字接尾辞（モブ・中学生A/B...）で区別
- 属性キーに特殊トークン`すべて`を追加。未所属キャラも含め無条件でマッチさせたい場合に使う（属性一致自動出現・行単位ランダム・`character_join`のtag_matchの計4箇所で共通対応）

**属性タグのフォールバック修正 + 退出キャラのログ/チャットバグ修正**
- 部屋＋World属性タグの合成を「合算」から「部屋側優先のフォールバック」に修正（World4データの意図が壊れていた問題を解消）。部屋のWorldアタッチ時・新規枠追加時に親の初期タグを自動コピー
- 退出済みキャラの過去チャット行が「???」表示になる不具合、退出後もLLM応答内で喋り続けてしまう不具合を修正（履歴に退出マーカーを挿入＋システムプロンプトに禁止リスト）
- タグ一致だけで（明示スロット割り当てなし）出現しうるキャラがCharactersPage上で「未所属」表示になっていた不具合を修正

**キャラ-World明示アタッチ + モブ複製インスタンスの状態独立化**
- `world_characters`（単純な中間テーブル）で、キャラをWorldへ明示的にアタッチ/デタッチできるように（既存の推定所属ロジックに追加、置き換えではない）
- モブの複製インスタンスが関係値・呼び方・ステータスを共有してしまう問題を修正——`relationship_states`等に`room_session_character_id`を追加し、`@モブ・中学生A`のような表示名指名でインスタンスを一意に特定できるように

**KoboldCppモデルピッカー・LLMサンプリング設定・周辺確認/買い物モード・通貨システム**
- 起動設定のモデルパスをテキスト入力からフォルダ一覧選択式に変更、Animaアーキテクチャ対応（`--sdvae`/`--sdclip1`）
- `llm_generation_settings`（temperature/rep_pen/top_p/top_k/min_p）を新設し、応答の多様性・繰り返し抑制をユーザー調整可能に
- `@周辺`メンション＋`room_template_item_categories`で、部屋ごとに「周辺確認」時のITEM_GRANT対象カテゴリを制限可能に。部屋に配置したPropもテキスト生成へ渡すように
- Worldごとの通貨（有効化・単位・初期所持金）、アイテム売買価格、`is_shop`部屋、売却UIを追加

**有料イベント + プライベート部屋**
- `has_money`条件／`spend_money`アクションを追加し、所持金を消費するイベントを組めるように
- `room_templates.suppress_auto_population`で、タグ一致・ランダム自動出現を丸ごと無効化できる部屋（プライベートホテル部屋など）を追加。SPEC.mdに商店街拡張コンテンツもあわせて記載

**部屋ランダム埋めのデータ消失バグ修正 + チャット/イベントUX改善7点**
- `RoomWorldConfigPage.jsx`のローカル編集状態管理が、最初の編集で枠マップ全体を固定化してしまい、保存時にconfigが再フェッチされると未編集の枠が消えるバグを修正
- @メンションボタンのトグル化、送信後メンション保持設定（`chat_input_settings`）、`generate_image`の取りこぼし防止タグの個別オプトアウト、`insert_dialogue`固定文への`${target1}`系プレースホルダー対応、メンションのみ送信を「継続」扱いに、`set_scene_situation`アクション新設、現在の時間帯/天候/季節をシステムプロンプトへ反映

**タグマッチ人数上限 + 行動コマンド部屋表示 + World⇔部屋間ナビゲーション**
- `world_room_templates.tag_match_max_count`で属性タグ一致による自動出現人数に上限を設定可能に（超過時は重み付きランダム抽選）
- `action_commands.visible_when_room_template_ids`で行動コマンドを特定の部屋にだけ表示可能に
- `RoomWorldConfigPage.jsx`から元のWorld編集画面へ戻れるリンクを追加

**LLM主導のステータス/関係値自動増減**
- 状態値（自己ステータス）は毎送信の応答生成に相乗りする形で`[STAT_CHANGE: ...]`タグとして申告させ（追加LLM呼び出しなし）、関係値は一定送信間隔ごと・セッション終了時に専用の低温度追加呼び出しで判定。どちらも軸ごとの`llm_auto_update_enabled`でオプトアウト可能

**アイテム使用時の重み付きランダム付与（`grant_random_item`）**
- 固定1種類のみだった`grant_item`と別に、複数候補から重み付き抽選で1つ付与するアクションを追加。World4の釣りコンテンツ（竿＋魚3種）で実演

**イベント複数選択の一括エクスポート**
- イベント一覧にチェックボックスを追加し、選択した複数イベントをまとめてzipエクスポート可能に（既存の`contentBundle`マニフェスト機構を再利用）。インポート欄もJSON単体/zipバンドルの両対応に拡張

**${target1}が同席他キャラの情報を巻き込むバグの修正 + 生成画像プロンプトの確認表示**
- `generate_image`のベースプロンプト（`character_tags`）が対象絞り込み（`candidateParticipants`）を無視し、常に部屋の同席者全員から組み立てられていたバグを修正
- 検証しやすいよう、チャット上の生成画像に「生成に使ったプロンプト」を折りたたみ表示する機能（既定OFF、設定画面でON/OFF可能）を追加

**その他の修正**
- koboldcppモデルパスの解決がnpm workspacesの実行cwd（`server/`）を考慮しておらず、パスが見つからない不具合を修正
- チャットの「名前のみ吹き出し」バグを修正、口調指示を追加、Gemma/Anima起動設定を追加
- 部屋作成が500エラーになる不具合（削除済み列への書き込み）を修正

## v0.1.2

実際にプレイしながらの動作確認フェーズで出た不具合報告・機能改善要望への対応。部屋のマスタデータ化、@メンション連動、衣装の重ね着表現、キャラ別フラグなど

**部屋のマスタデータ化（World単独所有からの脱却）**
- `room_templates.world_id`がNOT NULL FKだったため、同じ部屋を複数Worldで使うには全フィールド＋背景画像込みの完全複製が必要だった（実際にWorld④分割時に発生）。部屋を共有マスタデータへ変更し、name/location/atmosphere/background/attribute_tags/is_placeは部屋自身に残したまま、World単位で変動すべきものを新規の`world_room_templates`（多対多の所属）、`room_connections.world_id`（経路もWorld別）、`room_template_participant_slots`＋`world_room_slot_assignments`（マスタは属性キーでの抽象スロット、Worldごとに実キャラを割り当て）、`prop_categories`＋`room_template_prop_categories`＋`world_room_props`/`world_room_free_props`（設備もカテゴリ候補はマスタ、具体配置はWorld別）に分離
- マイグレーション0030は追加のみ＋旧1部屋1Worldデータからのバックフィル。0032が旧テーブル/列を削除するが、実プレイでの動作確認が済むまで意図的に未適用のまま保留
- 管理UI: 部屋テンプレート編集画面（マスタ）とWorld別部屋設定画面（`RoomWorldConfigPage.jsx`、経路・スロット割当・設備配置）に分割。部屋一覧はWorldごとにグルーピングし折りたたみ可能、どのWorldにも属さない部屋は「未分類」グループに

**イベント条件/アクションが「@メンション中のキャラ」をターゲットにできるように**
- `relationship_threshold`/`has_status`/`has_outfit`条件と`change_relationship`/`change_status`/`set_address`アクションに、既存の`any_present`/`all_present`と並ぶ`"mentioned"`センチネルを追加。`mentioned_limit`でメンション順の先頭何人までを対象にするか絞り込み可能
- `insert_dialogue`/`change_outfit`/`character_join`（specific）/`character_leave`（specific）にも単一ターゲット版の`"mentioned"`対応を追加。誰もメンションされていない場合はアクション自体をスキップ（`insert_dialogue`のnull=ナレーション扱いへの誤フォールバックを防止）
- 併せて`generate_image`の`target_character_ids`が空配列時に@メンションへフォールバックしない既存バグ（UIの既定値が`null`ではなく`[]`のため`?? mentionedCharacterIds`が効かなかった）を修正

**Worldバンドルにキャラ状態/しきい値トリガー/イベント定義を含める**
- Worldエクスポートに`character_statuses`/`axis_status_triggers`/`event_definitions`を含めるオプションを追加し、「全部込み」zipが実際に遊べる状態一式を含むように
- 副産物として発見した名前解決バグを修正：`resolveRoomTemplateAssociations`と`importEventDefinitionJson`が常にインストール全体を名前検索していたため、インポートバッチ内に同名エンティティがあると意図しない方へ紐づく不具合があった。バッチ内エンティティを優先する`preferredCharacters`/`preferredStatuses`/`preferredRoomTemplates`オプションを追加

**管理UI改善（World並び替え・表情マスター編集・行動コマンド分離）**
- `WorldsPage.jsx`に並び替え（名前／作成日／最終プレイ日時）を追加
- `ExpressionTypesPage.jsx`に編集機能（APIは既存、UIのみ未対応だった）と、LLM向けタグとは別に画像生成用danbooruタグを個別指定できる`danbooru_tag`列を追加（未設定時は従来通り`llm_tag_key`にフォールバックし既存データの生成結果は変化なし）
- 旧`ItemsPage.jsx`をアイテム管理と行動コマンド管理（新規`ActionCommandsPage.jsx`）に分割

**衣装の重ね着表現（上着・追加装備・下着）**
- 既存の`clothing_*_extra`タグ列を「上着・重ね着」（ジャケットなど）として再定義し、新たに本来の「追加装備」（鎧・銃ホルダーなど普段着でないもの）用の`clothing_*_equipment`列、および`underwear_upper`/`underwear_lower`（下半身は水着も含む想定）を追加。`OUTFIT_TAG_FIELDS`は13→19列に。CRUD・コンテンツバンドルはフィールドリスト駆動の実装のため無改修で対応
- ショット範囲プリセット（`upperbody`/`cowboyshot`/`lowerbody`/`fullbody`）に`_outer`/`_equipment`バリアントを追加。`_full`はベース＋上着＋追加装備の合成で、下着は意図的に含まない（`${target1.underwear_upper}`のように個別参照）

**脱衣状態のキャラステータス化**
- 新規テーブルは追加せず、既存`character_statuses`の`exclusive_group`排他機構を`undress_state`という予約グループ名で再利用。付随して、`statusSnapshotRepo.js`の`buildStatusSnapshot`が排他グループステータスを1つしか同時追跡できない（`relationship_stage`単数形）既存バグを発見・修正——`stages`配列化し、`関係`と`undress_state`など複数ファミリーが同時にアクティブでも取りこぼさないように
- `exclusive_group: 'undress_state'`のアクティブステータスは、`promptBuilder.js`のキャラクターカードに「現在の服装状態：{ステータス名}」として自動的に反映されLLMからも認識可能に（他のexclusive_groupファミリーは引き続きLLM非可視）

**キャラ別フラグ（セッション一時／ルート永続）**
- 既存の`session_flags`（ルート全体でグローバル共有、変更なし）とは独立した新規`character_flags`テーブルを追加。`character_status_states`の`persistence_scope`二層化（playthrough_id/room_session_idの排他キー）と同じパターンを再利用
- `flag_state`条件／`set_flag`アクションに任意の`character_id`（固定ID／`any_present`・`all_present`／`mentioned`）と`scope`（ルート永続／セッション内一時）を追加。未指定時は既存のグローバル挙動を完全維持

## v0.1.1

プリセットコンテンツ拡充フェーズ（デモWorld「現代学園ファンタジー」の部屋/キャラ/イベント拡充、①現代・②異能バトルへの分割）で発生した実装上の要望から生まれた、イベントエンジンの小さな機能追加

**季節／時間帯／天候のイベント条件対応**
- `playthroughs`は元々`current_season_index`/`current_time_slot_index`/`current_weather`を保持していたが、値が変化したこと自体を通知する仕組みがなく、イベント条件から季節・時間帯・天候を判定する手段がなかった
- `advanceTime()`/`createPlaythrough()`（`server/src/db/repositories/playthroughsRepo.js`）が、これらの値が変化するたびに`session_flags`へ`season`/`time_slot`/`weather`キーで現在値を自動反映するように変更。既存の`flag_state`イベント条件だけで「夜間限定」「雨の日限定」「冬季限定」等のイベントを組めるようになった

**新規イベント条件`has_outfit`（着用中の衣装判定）**
- `character_id`（特定キャラ or `any_present`）と`outfit_name`（例："水着"）を指定し、対象キャラが現在その名前の衣装を着用しているかを判定する条件を追加。衣装IDでなく**名前**でマッチするため、キャラごとに異なるIDを持つ「水着」等の衣装カテゴリを、キャラを問わず横断的に判定できる
- `event_conditions`テーブルのCHECK制約にマイグレーション0029で追加。EventsPage.jsxの条件エディタにも対応UIを追加

## v0.1.0

初期コンセプト（チャット中心のロールプレイ＋関係性変化）と、その深化として積み上げてきた自己ステータス／キャラ状態／関係・呼び方／部屋間移動／アイテム系／コンテンツ移植基盤が一区切りついたマイルストーンリリース

**名前重複キャラの識別子対応／画像生成タグの13カテゴリ化**
- 同じセッションに同名キャラが複数同席すると発言の帰属が衝突する既知の問題を解消。新規`participantNaming.js`の`withDisambiguatedNames`が参加者配列の順序に基づき2人目以降に`"名前(2)"`のような接尾辞を付与し、システムプロンプト生成（`promptBuilder.js`）と応答パース時の名前解決（`roomSessions.js`）の両方が同じロジックから同じ結果を得ることで衝突を防止
- `outfits.image_tags`（単一のフラットな文字列）を13カテゴリ（主たる特徴／髪型／服装の主たる特徴／服装顔回り／服装上半身／服装下半身／足回り／靴／追加服装顔回り／追加服装上半身／追加服装下半身／追加服装足回り／持ち物）に分割。既存データは「主たる特徴」へ移行
- イベント/シーン生成のプロンプトテンプレートで使う`${target1}`系プレースホルダーがカテゴリ単位（`${target1.hairstyle}`）・範囲単位（`${target1.upperbody}`/`_extra`/`_full`、`upperbody`/`cowboyshot`/`lowerbody`/`fullbody`の4範囲×3種）で個別のタグ集合を参照できるように拡張。`belongings`（持ち物）は位置が不定なためどの範囲プリセットにも含まれず常に個別参照
- キャラ編集画面の衣装タグ入力を13カテゴリの折りたたみエディタに刷新。あわせて、画像生成ボタンがフォーム上の現在値（未保存でも可）をリクエストに含めるように変更し、「入力したプロンプトが生成に反映されず一度保存が必要」だった不具合を解消——保存なしでプロンプト調整→生成を繰り返せるようになった
- キャラ／World/部屋テンプレートのzipバンドルexport/importも13フィールドに対応

**自己ステータス／キャラ状態システム**
- 既存`relationship_axes`/`relationship_states`を拡張し、`scope`列（`relationship`/`self_stat`）と`regen_per_time_slot`列（時間帯ごとの自然回復/減少、NULLで無効）を追加。既存の関係性軸6種はそのまま`scope='relationship'`として残し、新規に自己ステータス11種（体力・気力・満腹・水分・テンション・精神・道徳・善悪・従順・魅力・酔い）を`scope='self_stat'`として追加。時間経過（ターン数/部屋退出/`advance_time`アクションのいずれか）のたびに、対象プレイスルーに登場済みの全キャラへ`regen_per_time_slot`が自動適用される（min/max値でクランプ）
- 新規`character_statuses`（共通＋World固有の2段構成、気絶・死亡などカテゴリ的な状態のマスター）／`character_status_states`（付与状況の記録）を追加。ステータスごとに`persistence_scope`（`playthrough`＝永続／`session`＝部屋移動・再入室でリセット／`accompanying`＝同行中のみ継続）と`removes_from_session`（付与時にセッション参加者から自動除外するか）を個別設定可能
- 新規イベント条件`has_status`（対象キャラ＋ステータス＋所持していない場合に成立、の否定フラグ）／アクション`change_status`（grant/remove/lock/unlock）を追加し、既存の`has_item`/`change_relationship`と同じUXでイベントエディタから利用可能
- 新規`axis_status_triggers`（軸のしきい値超え/下回りに応じてステータスを自動付与・解除するマスター）を追加。`relationshipStatesRepo.js`の`adjustValue`（イベントの`change_relationship`アクション・自然回復のどちらもここを通る）にフックしたため、値の変化源を問わず一律にしきい値判定が働く。`character_status_states.locked`フラグがオンの間は自動解除をスキップ（明示的な`remove`操作は常に有効。自動付与は既にアクティブなステータスのロックを上書きしない）
- Room→Place移動時の同行キャラ引き継ぎ処理（`createRoomSession`の`carryOverParticipants`）を拡張し、`accompanying`スコープのキャラ状態（ロック状態含む）も新セッションへコピー。`session`スコープはリセットが目的のため引き継がれない
- 新規「キャラ状態」管理ページ（ステータス定義CRUD＋しきい値トリガー編集）を追加。関係性軸ページにも区分（関係性軸／自己ステータス）セレクタと自然増減入力欄を追加

**「関係」「呼び方」**
- `character_statuses`に`exclusive_group`列を追加。同じグループ名を持つステータスはキャラごとに常に1つだけアクティブになり、新しい付与で同グループの他のアクティブなステータスが自動解除される汎用の排他状態機構——「関係」（他人/顔見知り/恋人など）はこれを使って新規マスターなしで実現
- `axis_status_triggers`の評価順序をしきい値の厳しさ順にソートするよう変更。1回の値変化で複数のしきい値が同時に成立しても（例：好感度が一気に大きく変化）、排他グループ内は最も進んだ1段階だけが正しく残るように収束する。ロック中の段階は自動では一切変化しない（既存のロック哲学を排他グループの追い出しにも拡張）
- 新規`character_address_states`（プレイスルー×キャラごとの可変な「呼び方」）を追加。`character_statuses`の新規`default_address_on_grant`列（ステータス付与に連動する既定の呼び方）と、新規イベントアクション`set_address`（明示的な変更）の両方から更新可能——同じ値への上書きのため自然に「後勝ち」になる
- LLMへ送るキャラクター情報に、動的な呼び方が設定されていれば静的な設定値の代わりにそれを使用するよう`promptBuilder.js`を変更
- 「キャラ状態」管理ページに排他グループ／呼び方自動変更の入力欄、イベントエディタに`set_address`のパラメータ編集UIを追加

**チャット欄へのステータス表示**
- 3箇所（参加者ストリップ／折りたたみパネル／チャット欄の顔アイコン付近）×3項目（自己ステータス／キャラ状態／関係ステージ）を独立にON/OFFできる表示設定を追加。`worlds.status_display_settings`（World単位、見せてよい範囲の上限、既定は全OFF）と新規`status_display_preferences`（プレイヤー個人の絞り込み設定、既定は全ON）の2段構成——実効可否はサーバー側で両者をANDして`session.status_display_visibility`として1箇所にまとめて返す
- 参加者ストリップ・折りたたみパネルは各キャラの現在値をリアルタイム表示。チャット欄の顔アイコン付近は`messages.status_snapshot`に発言した瞬間の値を凍結して保存し、以降そのキャラのステータスが変化しても過去メッセージの表示だけは変わらない——チャットログがそのまま状態変化の記録になる
- Worldの編集フォーム・設定画面のそれぞれに3×3のチェックボックスグリッド（`StatusDisplayGrid`）を追加

**キャラ／World／部屋テンプレートの画像込みエクスポート・インポート**
- キャラシート＋立ち絵/表情画像一式、World（代表画像込み）、部屋テンプレート（背景画像込み）を、それぞれzipバンドル（manifest.json＋images/フォルダ）としてエクスポート/インポートできる機能を追加。新規依存`adm-zip`
- Worldエクスポートは所属する部屋テンプレートを含めるか、含める場合は登場キャラクターまで含めるかをエクスポート時にチェックボックスで選択可能。部屋テンプレート単体のエクスポートも登場キャラクターを含めるか選択可能
- インポート時、関係性軸・表情タイプ・画像スタイルプリセット・設備機材の参照はいずれも名前ベースで解決（IDはインストールごとに異なるため）。部屋テンプレートの登場キャラ・部屋間接続の参照解決は、インポートバッチ内の全キャラ・全部屋テンプレートの作成が完了した後にまとめて行う
- 部屋テンプレート単体（Worldデータを含まないバンドル）のインポートでは、対象Worldをインポート時に指定する（`target_world_id`）
- キャラクター／World／部屋テンプレートの各一覧画面にエクスポートボタンとインポート用ファイルピッカーを追加

## v0.0.5

ロードマップ項目の実装ラウンド

**チャット応答内での動的アイテム生成**
- LLM出力に`[ITEM_GRANT: アイテム名|カテゴリ名]: 説明文`タグを追加。既存の`[NARRATION]`/`[SCENE_CHANGE]`と同じ仕組みでパースし、キャラクターが物語上ユーザーに物を渡した場合のみ使うよう指示。カテゴリ名は現在のWorldで使えるカテゴリ一覧からLLMに選ばせ、一致しない/省略時は「未分類」にフォールバック
- 生成されたアイテムは常に現在の部屋が属するWorld固有枠に登録（グローバル共通枠には混ぜない）。同名アイテムはfind-or-createで1行に統合し、重複登録を防止
- 今回はテキストのみ（名前・説明文）。画像生成は見送り（既存の手動作成分含め、アイテムには元々画像機能自体がない）

**アイテムカテゴリ＋行動コマンドの自由拡張**
- アイテムに「共通＋World固有」の2段構成のカテゴリを新設（既存アイテムは移行時に「未分類」へ一括割当）。動的アイテム生成もこのカテゴリ一覧からLLMに選ばせる
- 既存の固定「📦 使う」アイコン1つに紐づいていた仕組みを撤廃し、`item_use`種別の行動コマンドをWorldごとに何個でも自由に定義できるように変更。各コマンドは「実行すると所持数を1減らす（消費型）」「実行すると対象キャラクターへ所有権を移す（渡す系）」を個別に指定可能（例：見せる＝どちらもオフ、捨てる＝消費のみ、渡す＝譲渡のみ）
- 消費判定は常にコマンド単位の設定が優先（カテゴリの消費型フラグは分類上の目安のみで、実行時の判定には使わない）
- チャット画面でアイテム系コマンドを開いた時点で、入力欄に`@名前`のメンションが既に入力されていれば対象を自動選択
- 移行時、既存の全`item_use`コマンドと同じスコープに「🤝 渡す」コマンドを自動追加し、既存の使い勝手を維持

**キャラクター応答の長さ調整**
- World設定に「応答の最大長さ（トークン数）」を追加（空欄で既定512）。チャット応答生成（`generateReply`）にのみ適用し、シーンタグ提案・キャラクター生成補助・イベント条件/アクション等の他のLLM呼び出しには影響しない

**関係性変化のアナウンス通知**
- World設定に「関係性の変化をチャット画面に通知する」トグルを追加（既定オフ）。オンの場合、`change_relationship`アクションの実行結果を`[SCENE_CHANGE]`通知と同じインライン表示でチャット画面に表示（例：「みおとの好感度が上がった」）
- 既存の不具合（SCENE_CHANGE通知が一度表示されると消えない）をあわせて修正。両通知とも6秒で自動的に消えるように変更

## v0.0.4

チャット関連機能の強化・調整ラウンド

**部屋＝「場所」の再検討（部屋間移動）**
- 部屋テンプレートに「他の部屋とつながりのある場所として扱う」フラグ（`is_place`）を追加。オンにすると部屋間の片方向の「つながり」（`room_connections`、移動先・ラベル・移動コストを個別設定）を編集できる
- World設定に「時間帯ごとの移動サブカウント上限」を追加。`is_place`の部屋では「退出する」の代わりに移動先ボタンが表示され、移動するとその接続の移動コストがルートのサブカウントに加算される。サブカウントが上限に達すると時間帯が1つ進み、超過分は切り捨ててリセットされる（時間経過を強制する既存の「退出する」とは別の、より細かい時間消費の仕組み）
- 移動は新しいセッションを作成する形を維持（既存の全リセット挙動を踏襲）しつつ、キャラクターに「同行させる」フラグを立てておくと移動先セッションにもそのキャラが引き継がれる（フラグのないキャラは現在の部屋に残る＝リセット）
- 終了したセッションが閲覧不能になっていた点を解消するため、ルートごとのセッション履歴一覧・閲覧専用ログビューアを新規追加（ルート選択画面から「会話ログを見る」でアクセス）

**チャット画面**
- 画面全体を使うフルスクリーンレイアウトに変更（ヘッダー・ステータス行・入力欄は固定、メッセージ一覧のみ内部スクロール）
- キャラクターアバターを28px円形から56px角丸表示に変更し、実際の立ち絵素材の縦横比に合わせやすく調整
- LLM応答を「全文受信後に一括パース」ではなく、`[Name]: text`等のスクリプト行を1行確定するごとに即座に永続化・配信する方式に変更。吹き出し／ナレーションが少しずつ順番に表示されるようになった
- キャラクター名は認識できるがEMOTIONタグが未知の場合、これまでの「行全体をナレーション扱い」から、「セリフ本文は活かしつつ表情画像のみ既定表情にフォールバックし、未知タグは本文末尾に`（tag）`として残す」方式に変更
- チャット欄で空欄のまま送信すると、これまでは何も起きなかったのを「ユーザーの行動・発言指示なしで直前の会話の続きを生成する」明示的な操作として扱うように変更。空欄送信時はユーザーメッセージを一切保存せず（チャット欄に表示される行動として残らない）、LLM呼び出し用のメッセージ配列にのみ最小限の継続用ターンを一時的に追加。末尾がassistantロールで終わる配列は常に空応答を返すことを実機検証で確認したため、この一時ターンが必要と判明

**アイテム・行動コマンド・メンション機能を新規追加**
- アイテムを構造化データとして管理（World共通＋World固有の2段構成、現代世界観に魔法アイテムが紛れ込む等を防止）。プレイスルーごとの所持品テーブルを追加
- 行動コマンド（キーワード送信／アイテム入手・確認・使用／自由入力）も同じ共通＋World固有の2段構成で管理し、アイコン付きのクイック操作バーとしてチャット画面に表示。自由入力枠は専用パネル（`FreeActionPanel`）として実装し、決まったコマンドにない行動もその場で送信可能に
- Teams風の@メンションでチャット発言・行動の対象キャラクターを明示指定できる機能を追加。イベント側のターゲット選択（`generate_image`の対象キャラ解決等）にも流用
- イベント条件に`has_item`（所持アイテム判定）、アクションに`grant_item`／`remove_item`を追加
- 代表的な行動コマンドのサンプル（調べる／見る／触る／待つ／挨拶する／持ち物／拾う／使う／自由入力、全てWorld共通）とサンプルアイテム（共通2件＋現代学園ファンタジー固有2件）をデモデータとして追加。「待つ」は時間を飛ばす操作ではなく「そのまま今の状況が続くのを見守る」ニュアンスの文面に調整
- ロードマップに「チャット応答内での動的アイテム生成」を追加（今回は未実装、将来の設計対象）

**イベント条件UIの操作性改善**
- 条件のキーワード入力がEnterキー依存だった箇所に、明示的な「+追加」ボタンを追加（モバイルでEnterが次フィールド送りになる問題への対応）

**イベントの成功／失敗分岐**
- 条件に`phase`（発火条件／結果判定条件）を追加し、イベント発火後に別条件セットで成功・失敗を判定できる仕組みを追加（既定は無効で既存イベントの挙動に影響なし）
- アクションに`outcome`（常に実行／成功時のみ／失敗時のみ）を追加し、判定結果に応じて実行するアクションを分岐可能に
- 結果判定条件に新条件タイプ`llm_judge`を追加。既存の決定的な条件タイプ（`has_item`等）と組み合わせて、LLMによるyes/no判定を結果判定に利用できる

**イベント連鎖**
- イベント定義に前提イベント参照（`prerequisite_event_definition_id`）と前提イベントの結果条件（問わない／成功時のみ／失敗時のみ）を追加。前提イベントが同一ルート内で（指定した結果条件を満たして）発火するまで、後続イベントは発火対象にならない
- `event_fire_history`に発火時点の`outcome`を新たに記録するよう変更（既存の成功/失敗分岐機能では結果をメモリ上でしか保持しておらず、前提イベント判定に必要な永続化が漏れていたための追加）

**KoboldCpp起動設定**
- 画像生成モデルの「fp8ロード」要望を調査した結果、導入済みKoboldCpp（v1.116.1）にはfp8ロードモード自体が存在せず、`--sdquant`（0=オフ／1=q8／2=q4）のみ対応と判明。代替として`--sdquant`を設定画面（アプリ内「KoboldCppを起動」ボタン付近）から選択できるように追加し、`koboldcppLauncher.js`と配布用`start-koboldcpp.bat`の両方に反映
- テキスト／画像生成モデルのパスを直接指定できる設定を追加（空欄なら従来通り`models/llm`・`models/sd`内の最初のファイルを自動使用）。指定したパスが存在しない場合は起動時にエラーを返す
- 画像生成用LoRA（`--sdlora`/`--sdloramult`）のパス・適用強度を指定できる設定を追加。高速化用LoRAを内包していないSDモデルでも高速化を効かせられるように。配布用`start-koboldcpp.bat`側は、同一の`if`ブロック内で`%SD_ARGS%`を複数回積み上げると先に設定した値が展開されずに消える、というバッチスクリプトの遅延展開に絡む不具合を発見・修正しつつ対応
- LoRA有効時にKoboldCppが起動失敗する不具合を修正。実際のエラーログから、KoboldCppは`--sdlora`と`--sdquant`（量子化ロード）の同時指定を許可していない（`argument --sdlora: not allowed with argument --sdquant`）ことが判明。この組み合わせを検出したら起動前にエラーを返すよう`koboldcppLauncher.js`を修正し、設定画面にも警告表示、`start-koboldcpp.bat`にも同様のチェックを追加。修正後、実機でLoRA読み込み成功（`Load Text Model OK: True` / `Load Image Model OK: True`）を確認

**画像生成設定の追加検討・実装**
- 画像種別ごとの生成設定にネガティブプロンプトを追加。これまで全ての画像生成呼び出しがKoboldCpp接続コード内のハードコード値（`worst quality, low quality`）を無条件で使っていたのを、種別ごとに編集・保存できるように変更
- 参照アンカーインペイントのアンカー列幅を、固定サイズ（種別ごとの設定値、既定200px）による`fit: 'cover'`強制クロップから、参照画像自身の実サイズ・アスペクト比に基づく完全動的サイズに変更。複数キャラの参照画像を使うシーン/イベント画像生成では、各画像のアスペクト比から算出した幅のうち最大値を共通のアンカー列幅とし、それより狭い画像は中央寄せで配置。既存の「アンカー幅」設定項目はUI上に残るが実際のサイズ計算には使われなくなった

**イベント定義のJSONエクスポート/インポート**
- イベント編集画面にエクスポート（JSONダウンロード）・インポート（JSONファイル読み込み）機能を追加。まずはイベント定義単体（条件・アクション含む）を対象とし、アイテム/行動コマンド等への対応は今回は見送り
- 条件・アクション内のキャラ参照（`character_id`・`candidate_character_ids`・`target_character_ids`）、対象部屋（room_template）、前提イベント参照は、エクスポート時にIDではなく名前を併記し、インポート先のインストールで名前が一致するものに解決し直す方式に。名前が見つからない場合はキャラ参照は未設定に、対象部屋・前提イベントはそれぞれglobalスコープ化・前提解除にフォールバックし、インポート後に警告で通知

**キャラクター属性キーの自動マッチング**
- キャラクター・World・部屋テンプレートに自由記述の「属性キー」（カンマ区切り、既存の`image_tags`と同じ方式）を追加。既存のTagChips UIコンポーネントで編集
- イベントアクション`character_join`に新しい選出方法「属性キー一致」（`selection_mode: 'tag_match'`）を追加。部屋またはそのWorldの属性キーとキャラクターの属性キーが重なるキャラから、既存の重み付き抽選ロジックで自動選出（候補キャラの手動指定が不要に）
- 部屋テンプレート編集画面の「初期参加キャラクター」一覧に、属性キーが一致するキャラを★印でハイライト表示（あくまで目視確認用の補助表示で、実際の自動登場ロジックは上記の`tag_match`選出方法経由）
- `character_join`の「指定」モードに、対象キャラの属性キーが部屋/Worldと一致しない場合は入室させないガードを追加。不一致時は既定または任意のナレーションで拒否を明示。イベントを何が発火させたか（キーワード条件、自由行動、@メンション等）に関わらず同じ`character_join`アクション実行を通るため、これ一箇所の対応で全トリガー経路をカバー

## v0.0.3

**主人公（あなた）機能**
- World単位で主人公の既定設定（名前・あだ名・性別・職業・容貌・その他自由記述）を保持し、ルート（Playthrough）ごとに継承／独自設定への上書きを選択可能に（SPEC.md 3.2.1）
- 性格・話し方・行動はあえて項目化せず、プレイヤー自身の発言に完全に委ねる設計
- `protagonist_mode`として「登場人物として参加する（character）」「ナレーター／神視点（narrator）」の2モードを追加。narratorモードはユーザーの発言を場面・NPCへの直接指示として扱う、機能追加前の挙動に相当
- 主人公の名前をLLMに教えたことで、モデルが`[名前]: セリフ`形式で主人公自身の発言を生成してしまう問題を確認。システムプロンプトでの明示的な禁止指示に加え、LLM出力パース後のコード側でも主人公名に一致する発言ターンを検出・破棄する二重の安全策を実装

**画像生成設定の拡張**
- 画像種別（立ち絵／表情差分／シーン／イベント／部屋の背景／World代表画像）ごとに、生成方式（`anchor_i2i` / `prompt_only`）・プロンプトテンプレート・キャンバスサイズ・SDパラメータ（steps/cfg_scale/denoising_strength）を個別設定できる機能を追加
- KoboldCppの`/sdapi/v1/samplers`と連携したサンプラー選択を種別ごとに追加
- プロンプトテンプレート内の使用可能変数（`${style_preset}`等）をタップ/ホバーで説明表示するUIを追加（PC・スマホ両対応）
- 種別ごとの設定画面に「この設定でテスト生成」機能を追加。編集中（未保存でも可）の設定を、その種別が参照する実データのうち最も若いIDのレコードをサンプルとして実際に生成・プレビューできる（保存されない使い捨て出力）

**KoboldCpp起動の導線**
- `koboldcpp/start-koboldcpp.bat`：実行ファイル・モデルファイルを自動検出して起動するバッチスクリプトを追加（開発環境・配布パッケージ双方に同梱）
- 設定画面に「KoboldCppを起動」ボタンを追加。サーバーと同じPC上でkoboldcpp.exeをデタッチ起動し、`node --watch`によるサーバー再起動の影響を受けない

## v0.0.2

- 配布用リリースパッケージ作成ツール（`create-release-package.bat`）を追加。アプリ本体とデモデータを別フォルダへコピーし、`koboldcpp/`は空フォルダとして再作成、対象マシン向けの`setup-and-start.bat`とKoboldCppセットアップ案内を同梱
- Viteの開発プロキシがバックエンドポートをハードコードしており、`/images`もプロキシ対象外だった不具合を修正。別`PORT`で起動した2台目のインスタンスが、意図せず1台目のバックエンドを参照してしまい、生成画像が描画されない問題を解消（`.env`のPORTを直接読み込む方式に変更）

## v0.0.1

- KoboldCppとのテキスト／画像生成連携、複数キャラクターのスクリプト形式レスポンス解析
- 参照アンカー方式によるシーン・イベント・立ち絵画像生成パイプライン
- イベントルールエンジン（条件／アクション／エディタUI）
- 設定画面（KoboldCpp接続状況・LAN情報・画像スタイルプリセット・種別ごとの出力形式・テスト生成ツール）
- レスポンシブレイアウト調整、デモ用世界観コンテンツの拡充
