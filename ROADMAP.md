# 実装状況・ロードマップ

機能単位で整理した実装状況のサマリー。個々の変更の技術的詳細は [CHANGELOG.md](CHANGELOG.md)、仕様は [SPEC.md](SPEC.md) を参照。

## リリース済み

### v0.0.5

**チャット応答内での動的アイテム生成**
- LLM出力の`[ITEM_GRANT: アイテム名|カテゴリ名]: 説明文`タグをパースし、現在の部屋のWorld固有枠にfind-or-createでアイテムを登録・付与。テキストのみ（画像生成は見送り）

**キャラクター応答の長さ調整**
- World設定に「応答の最大長さ（トークン数）」を追加（chat_reply専用、既定512）

**関係性変化のアナウンス通知**
- World設定のトグルで、関係性変化をSCENE_CHANGEと同様のインライン通知としてチャット画面に表示。あわせてSCENE_CHANGE通知が消えない不具合も修正（両方とも6秒で自動消去）

**アイテムカテゴリ＋行動コマンドの自由拡張**
- アイテムに共通＋World固有の2段構成のカテゴリを新設。動的アイテム生成もこのカテゴリ一覧からLLMに選ばせる
- 固定の「使う」アイコン1つだった仕組みを撤廃し、`item_use`種別の行動コマンドをWorldごとに自由に何個でも定義可能に。各コマンドで「消費型として扱うか」「対象へ所有権を移すか（渡す系）」を個別指定。消費判定は常にコマンド単位の設定が優先（カテゴリ側は分類の目安のみ）
- アイテム系コマンドを開いた時点で、入力欄に`@名前`のメンションが既にあれば対象を自動選択

### v0.0.4

**部屋＝「場所」の再検討（部屋間移動）**
- 部屋テンプレートに`is_place`フラグ＋片方向の「つながり」（`room_connections`：移動先・ラベル・移動コスト）
- World単位の「時間帯ごとの移動サブカウント上限」。移動は時間を1コマ強制せず、コストを積算して上限到達時のみ時間帯が進む
- 「同行させる」フラグでキャラを移動先セッションに引き継ぎ可能
- 終了済みセッションを閲覧できるセッション履歴一覧・閲覧専用ログビューアを新設

**チャット画面**
- フルスクリーンレイアウト化、アバター表示の大型化（56px角丸）
- LLM応答をスクリプト行単位で逐次パース・配信（一括表示から段階表示へ）
- 未知EMOTIONタグのフォールバック方式改善（セリフは活かし表情のみ既定に）
- 空欄送信＝「続きを生成」の明示的操作として扱う仕様追加

**アイテム・行動コマンド・メンション機能**
- アイテムの構造化管理（World共通＋World固有）、プレイスルー単位の所持品
- アイコン付きクイック操作バー、自由入力パネル
- Teams風@メンションによる対象キャラ明示指定
- イベント条件`has_item`、アクション`grant_item`/`remove_item`

**イベント機能強化**
- キーワード条件の「+追加」ボタン（モバイルのEnter問題対応）
- 成功／失敗の結果分岐（`phase`条件、`outcome`アクション、`llm_judge`条件）
- イベント連鎖（前提イベント参照＋結果条件）
- イベント定義のJSONエクスポート/インポート（キャラ・部屋・前提イベント参照の名前ベース解決）

**KoboldCpp起動設定**
- `--sdquant`選択、モデルパス直接指定
- 画像生成用LoRAのパス・強度指定、LoRA×sdquant併用不可の検出とエラー化

**画像生成設定**
- 種別ごとのネガティブプロンプト設定
- 参照アンカー幅の完全動的化（参照画像の実サイズ・アスペクト比ベース）

**キャラクター属性キー**
- キャラクター／World／部屋テンプレートへの自由記述「属性キー」追加
- `character_join`の属性キー一致による自動選出（`tag_match`）、初期参加キャラ一覧での★ハイライト
- 属性不一致キャラの入室拒否ガード

### v0.0.3
- **主人公（あなた）機能**：World単位の既定設定＋ルートごとの上書き、`character`/`narrator`（神視点）モード、主人公の自己発言をシステムプロンプト＋パース後コードの二重で抑止
- **画像生成設定の拡張**：画像種別ごとに生成方式（アンカーi2i／プロンプトのみ）・プロンプトテンプレート・キャンバスサイズ・SDパラメータ・サンプラーを個別設定、種別ごとのテスト生成機能
- **KoboldCpp起動導線**：`start-koboldcpp.bat`の自動検出起動、アプリ内「KoboldCppを起動」ボタン（デタッチ起動）

### v0.0.2
- 配布用リリースパッケージ作成ツール
- 開発プロキシのポート固定不具合を修正（複数インスタンス同時起動時の画像取得ミス解消）

### v0.0.1
- KoboldCpp連携（テキスト／画像生成）、複数キャラクターのスクリプト形式レスポンス解析
- 参照アンカー方式による立ち絵・シーン・イベント画像生成パイプライン
- イベントルールエンジン（条件／アクション／エディタUI）
- 設定画面（接続状況・LAN情報・画像スタイルプリセット・出力形式・テスト生成）
- レスポンシブレイアウト、デモ用世界観コンテンツ

---

## 未リリース（作業中・バージョン未確定）

**自己ステータス／キャラ状態システム**
- 自己ステータス（体力／気力／満腹／水分／テンション／精神／道徳／善悪／従順／魅力／酔いの11種）を既存`relationship_axes`の`scope`拡張（`relationship`/`self_stat`）で追加。時間帯ごとの自然回復／減少（`regen_per_time_slot`）をプレイスルー内の全キャラ（セッション不参加でも対象）に適用
- 新規`character_statuses`／`character_status_states`（気絶・死亡等のカテゴリ的状態）。共通＋World固有の2段構成。`persistence_scope`で永続（`playthrough`）／セッション単位（`session`）／同行中のみ継続（`accompanying`）を切り替え、`removes_from_session`でステータス付与時のセッション参加者からの自動除外をステータスごとに個別設定
- 新規イベント条件`has_status`（対象キャラ＋ステータス＋否定）／アクション`change_status`（grant/remove/lock/unlock）
- `axis_status_triggers`：軸のしきい値超え／下回りに応じてステータスを自動付与・解除。`locked`中は自動解除をスキップ（明示的な`remove`操作は常に有効。自動付与は既にアクティブなステータスのロックを上書きしない）
- Room→Place移動時、`accompanying`スコープのステータスは同行フラグの立ったキャラに引き継がれる（`session`スコープはリセットが本来の目的のため引き継がれない）
- 管理UI：「キャラ状態」ページ（ステータス定義CRUD＋しきい値トリガー編集）を新設、関係性軸ページに区分（関係性軸／自己ステータス）・自然増減欄を追加、イベントエディタに`has_status`/`change_status`のパラメータ編集UIを追加

**「関係」「呼び方」**
- **関係**（他人/顔見知り/恋人など）は新規マスターを作らず、`character_statuses`に`exclusive_group`列を追加して実現。同じグループ名を持つステータスはキャラごとに常に1つだけアクティブになり、新しいステータスを付与すると同グループの他のアクティブなステータスは自動解除される（「関係」専用ではなく汎用の排他状態機構）
- 複数のしきい値が一度に成立するケース（好感度が一気に大きく変化する等）でも、`axis_status_triggers`がトリガーを厳しさ順に処理することで、排他グループ内は常に最も進んだ1段階だけが残るように収束する。ロック中の段階は自動では一切変化しない（明示的な`change_status`操作でのみ変更可能）——既存のロック哲学をそのまま拡張
- **呼び方**（キャラがプレイヤーをどう呼ぶか）は新規`character_address_states`（プレイスルー×キャラごとに1つの可変文字列）で実現。`character_statuses`の`default_address_on_grant`列（関係ステージの付与に連動する既定の呼び方）と、新規イベントアクション`set_address`（明示的な変更）の両方から更新可能——同じ1つの値への上書きのため自然に「後勝ち」になる
- LLMへ送るキャラクター情報（`characterSheetFormat.js`のキャラカード）は、動的な呼び方が設定されていれば静的な`call_user_as`の代わりにそれを使用する
- 管理UI：「キャラ状態」ページに排他グループ／呼び方自動変更の入力欄を追加、イベントエディタに`set_address`のパラメータ編集UIを追加

**チャット欄へのステータス表示**
- 3箇所（参加者ストリップ／折りたたみパネル／チャット欄の顔アイコン付近）×3項目（自己ステータス／キャラ状態／関係ステージ）を独立にON/OFFできる表示設定。World単位の`status_display_settings`（見せてよい範囲の上限、既定は全OFF）と、プレイヤー個人の`status_display_preferences`（さらに絞り込む個人設定、既定は全ON）の2段構成——実効可否はサーバー側で両者をANDして算出
- 参加者ストリップ・折りたたみパネルは常時「現在値」を表示。チャット欄の顔アイコン付近は`messages.status_snapshot`に発言した時点の値を凍結して保存し、以降そのキャラのステータスが変化しても過去メッセージの表示は変わらない——会話ログとして機能する
- 管理UI：Worldの編集フォーム、設定画面にそれぞれ3×3のチェックボックスグリッドを追加

---

## ロードマップ（未着手・設計待ち）

### コンテンツパイプライン
- **TTS対応** — KoboldCpp自体は`--ttsmodel`等のCLIフラグを既に持っている（未使用）。モデル配置・API連携・再生UIが必要
- **名前重複キャラの識別子** — `generateReply()`の`participantsByName`が表示名でキャラを引き当てており、同名キャラが複数いると衝突する
- **SillyTavernカード形式インポート＋和訳** — `characterSheetFormat.js`の`parse()`を新フォーマット対応として拡張、英語データの自動和訳ステップを追加
- **キャラごとの画像込みフォルダインポート** — キャラシートJSON＋立ち絵/表情画像一式をフォルダ単位で持ち運び可能に。上記2項目と合わせて「キャラの持ち運び」という共通テーマ
- **Qwen-Image-Edit対応** — KoboldCpp本体はv1.101.1以降で公式サポート済み（開発者本人が動作確認済み）。ただし本プロジェクトの手元検証では、非公式「AIO」モデル一式（VAE・テキストエンコーダ未統合、mmprojの変換方式がKoboldCppのローダーと非互換）は不可と判明。公式リンク先の構成一式（QuantStack + mradermacher、計約16.6GB）は未検証のまま
