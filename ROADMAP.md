# 実装状況・ロードマップ

機能単位で整理した実装状況のサマリー。個々の変更の技術的詳細は [CHANGELOG.md](CHANGELOG.md)、仕様は [SPEC.md](SPEC.md) を参照。

## リリース済み

### v0.1.4

実プレイのバグ報告・UI/UX要望・データ管理要望への一括対応

**EventsPageモバイルレイアウト修正＋サイドバー高さ制限** — `sidebar-pane`のflex方向指定漏れを修正、デスクトップでは80vhで独立スクロール化
**@メンション入力の部屋移動リセット** — 部屋移動後に入力欄へ残っていた不要な@メンションをクリア
**キャラ状態一覧の上半身/下半身バッジ** — 同名ステータスを色分けバッジで判別可能に
**並び順・折りたたみ状態のlocalStorage永続化** — `useLocalStorageState`フックを新設、一覧画面の表示状態がリロード後も保持
**チャット参加キャラ欄の折りたたみ化＋@メンションバー横スクロール化** — 参加人数が多い部屋でのチャットログ圧迫を解消
**画像生成の天候・時間帯danbooruタグ対応** — World単位で天候/時間帯ごとにタグを設定、`${weather_tags}`/`${time_slot_tags}`として利用可能に
**ルート（プレイスルー）削除機能** — 未配線・未動作だった`deletePlaythrough`を修正しUIに配線

### v0.1.3

実プレイ継続フェーズでの機能拡充・不具合修正

**管理一覧画面の統一** — Characters/Events/Itemsをサイドバー＋編集ペイン構成に統一、モバイル用スライドアウト一覧を追加
**脱衣状態の4トラック化** — 上半身/下半身×衣服/下着を独立管理、状態に応じた衣装タグ抑制、World4コンテンツ拡充
**行動コマンドの3階層カテゴリ化** — カテゴリ／サブカテゴリのピル切替、ステータス条件付き表示
**イベント発火のセッション単位リセット** — クールダウン・最大発火回数・前提連鎖チェックを部屋滞在単位でもリセット可能に
**曜日・祝日カレンダー** — 季節/時間帯/天候と同じ設計で曜日・個別休日を追加
**@メンション順序バグの修正（2箇所）** — `resolveMentions()`と、その先の`${target1}`解決3箇所（generate_image/insert_dialogue/set_scene_situation）
**部屋スロットの複数キャラ＋時間帯制御** — 1枠に複数キャラ・時間帯制限、キャラ状態のWorld共有マスタ化
**モブキャラ概念** — 関係値・自己ステータス・呼び方がセッション単位でリセットされるキャラ種別を追加
**参加者ランダム割り当ての行単位再設計** — 0〜N人の変動表現、モブの複数選出、`すべて`ワイルドカードタグ
**属性タグのフォールバック修正** — 部屋＋World属性タグを合算でなく部屋優先のフォールバックに。退出キャラのログ/チャット不具合も修正
**キャラ-World明示アタッチ** — 推定所属とは別に明示的な付け外しが可能に。モブ複製インスタンスの状態も個体別に分離
**通貨・買い物・周辺確認モード** — KoboldCppモデルピッカー、LLMサンプリング設定、Prop連携、通貨システムを追加
**有料イベント＋プライベート部屋** — `has_money`/`spend_money`、自動出現を無効化できる部屋
**部屋ランダム埋めのデータ消失バグ修正＋チャット/イベントUX改善7点**
**タグマッチ人数上限＋行動コマンド部屋表示**
**LLM主導のステータス/関係値自動増減** — 毎送信相乗り（状態値）／専用低温度呼び出し（関係値）
**重み付きランダムアイテム付与`grant_random_item`**
**イベント複数選択の一括エクスポート**
**${target1}が同席他キャラを巻き込むバグの修正＋生成画像プロンプトの確認表示**（既定OFF）

### v0.1.2

実プレイでの動作確認フェーズで出た不具合報告・改善要望への対応

**部屋のマスタデータ化** — 部屋が複数Worldで共有できる共有マスタデータに。経路・参加者スロット・設備配置はWorld別の別テーブルへ分離
**イベントの@メンションターゲット対応** — 条件/アクションが「@メンション中のキャラ」を`any_present`/`all_present`と並ぶセンチネルとして指定可能に
**Worldバンドル拡充** — エクスポートにキャラ状態/しきい値トリガー/イベント定義を含められるように。副産物でインポート時の名前解決バグを修正
**管理UI改善** — World並び替え、表情マスター編集＋画像生成タグ、行動コマンド画面の分離
**衣装の重ね着表現** — 上着・重ね着／追加装備／下着（19カテゴリに拡張）とショット範囲プリセットの`_outer`/`_equipment`バリアント
**脱衣状態のキャラステータス化** — `character_statuses`の排他グループ機構を再利用し、LLMのシステムプロンプトにも反映
**キャラ別フラグ** — ルート全体でグローバル共有だった既存フラグとは独立に、セッション一時／ルート永続のキャラ別フラグを追加

### v0.1.1

**季節／時間帯／天候のイベント条件対応**
- `advanceTime()`/`createPlaythrough()`が、季節・時間帯・天候の変化を`session_flags`（`season`/`time_slot`/`weather`）へ自動反映。既存の`flag_state`条件だけで「夜間限定」「雨の日限定」「冬季限定」イベントを組めるように

**新規イベント条件`has_outfit`**
- 対象キャラが特定名の衣装（例："水着"）を現在着用しているかを判定。衣装IDでなく名前でマッチするため、キャラを問わず横断的に判定可能

### v0.1.0

初期コンセプト（チャット中心のロールプレイ＋関係性変化）の深化が一区切りついたマイルストーンリリース。次フェーズはRPG/TRPG的な遊び方の強化に入る前に、遊びながらのデバッグとプリセットコンテンツの拡充を行う方針。

**名前重複キャラの識別子対応／画像生成タグの13カテゴリ化**
- 同じセッションに同名キャラが複数同席すると発言の帰属が衝突する既知の問題を解消。新規`participantNaming.js`の`withDisambiguatedNames`が参加者配列の順序に基づき2人目以降に`"名前(2)"`のような接尾辞を付与し、システムプロンプト生成（`promptBuilder.js`）と応答パース時の名前解決（`roomSessions.js`）の両方が同じロジックから同じ結果を得ることで衝突を防止
- `outfits.image_tags`（単一のフラットな文字列）を13カテゴリ（主たる特徴／髪型／服装の主たる特徴／服装顔回り／服装上半身／服装下半身／足回り／靴／追加服装顔回り／追加服装上半身／追加服装下半身／追加服装足回り／持ち物）に分割。既存データは「主たる特徴」へ移行
- イベント/シーン生成のプロンプトテンプレートで使う`${target1}`系プレースホルダーがカテゴリ単位（`${target1.hairstyle}`）・範囲単位（`${target1.upperbody}`/`_extra`/`_full`、`upperbody`/`cowboyshot`/`lowerbody`/`fullbody`の4範囲×3種）で個別のタグ集合を参照できるように拡張。`belongings`（持ち物）は位置が不定なためどの範囲プリセットにも含まれず常に個別参照
- キャラ編集画面の衣装タグ入力を13カテゴリの折りたたみエディタに刷新。あわせて、画像生成ボタンがフォーム上の現在値（未保存でも可）をリクエストに含めるように変更し、「入力したプロンプトが生成に反映されず一度保存が必要」だった不具合を解消——保存なしでプロンプト調整→生成を繰り返せるようになった
- キャラ／World/部屋テンプレートのzipバンドルexport/importも13フィールドに対応

**自己ステータス／キャラ状態システム**
- 既存`relationship_axes`/`relationship_states`を拡張し、`scope`列（`relationship`/`self_stat`）と`regen_per_time_slot`列（時間帯ごとの自然回復/減少、NULLで無効）を追加。既存の関係性軸6種はそのまま`scope='relationship'`として残し、新規に自己ステータス11種（体力・気力・満腹・水分・テンション・精神・道徳・善悪・従順・魅力・酔い）を`scope='self_stat'`として追加。時間経過（ターン数/部屋退出/`advance_time`アクションのいずれか）のたびに、対象プレイスルーに登場済みの全キャラへ`regen_per_time_slot`が自動適用される（min/max値でクランプ）
- 新規`character_statuses`（共通＋World固有の2段構成、気絶・死亡などカテゴリ的な状態のマスター）／`character_status_states`（付与状況の記録）を追加。ステータスごとに`persistence_scope`（`playthrough`＝永続／`session`＝部屋移動・再入室でリセット／`accompanying`＝同行中のみ継続）と`removes_from_session`（付与時にセッション参加者から自動除外するか）を個別設定可能
- 新規イベント条件`has_status`（対象キャラ＋ステータス＋所持していない場合に成立、の否定フラグ）／アクション`change_status`（grant/remove/lock/unlock）を追加し、既存の`has_item`/`change_relationship`と同じUXでイベントエディタから利用可能
- 新規`axis_status_triggers`（軸のしきい値超え/下回りに応じてステータスを自動付与・解除するマスター）を追加。`relationshipStatesRepo.js`の`adjustValue`（イベントの`change_relationship`アクション・自然回復のどちらもここを通る）にフックしたため、値の変化源を問わず一律にしきい値判定が働く。`character_status_states.locked`フラグがオンの間は自動解除をスキップ（明示的な`remove`操作は常に有効。自動付与は既にアクティブなステータスのロックを上書きしない）
- Room→Place移動時の同行キャラ引き継ぎ処理（`createRoomSession`の`carryOverParticipants`）を拡張し、`accompanying`スコープのキャラ状態（ロック状態含む）も新セッションへコピー。`session`スコープはリセットが目的のため引き継がれない
- 新規「キャラ状態」管理ページ（ステータス定義CRUD＋しきい値トリガー編集）を追加。関係性軸ページにも区分（関係性軸／自己ステータス）セレクタと自然増減入力欄を追加

**「関係」「呼び方」**
- `character_statuses`に`exclusive_group`列を追加。同じグループ名を持つステータスはキャラごとに常に1つだけアクティブになり、新しい付与で同グループの他のアクティブなステータスが自動解除される汎用の排他状態機構——「関係」（他人/顔見知り/恋人など）はこれを使って新規マスターなしで実現
- `axis_status_triggers`の評価順序をしきい値の厳しさ順にソートするよう変更。1回の値変化で複数のしきい値が同時に成立しても（例：好感度が一気に大きく変化）、排他グループ内は最も進んだ1段階だけが正しく残るように収束する。ロック中の段階は自動では一切変化しない（既存のロック哲学を排他グループの追い出しにも拡張）
- 新規`character_address_states`（プレイスルー×キャラごとの可変な「呼び方」）を追加。`character_statuses`の新規`default_address_on_grant`列（ステータス付与に連動する既定の呼び方）と、新規イベントアクション`set_address`（明示的な変更）の両方から更新可能——同じ値への上書きのため自然に「後勝ち」になる
- LLMへ送るキャラクター情報に、動的な呼び方が設定されていれば静的な設定値の代わりにそれを使用するよう`promptBuilder.js`を変更
- 「キャラ状態」管理ページに排他グループ／呼び方自動変更の入力欄、イベントエディタに`set_address`のパラメータ編集UIを追加

**チャット欄へのステータス表示**
- 3箇所（参加者ストリップ／折りたたみパネル／チャット欄の顔アイコン付近）×3項目（自己ステータス／キャラ状態／関係ステージ）を独立にON/OFFできる表示設定を追加。`worlds.status_display_settings`（World単位、見せてよい範囲の上限、既定は全OFF）と新規`status_display_preferences`（プレイヤー個人の絞り込み設定、既定は全ON）の2段構成——実効可否はサーバー側で両者をANDして`session.status_display_visibility`として1箇所にまとめて返す
- 参加者ストリップ・折りたたみパネルは各キャラの現在値をリアルタイム表示。チャット欄の顔アイコン付近は`messages.status_snapshot`に発言した瞬間の値を凍結して保存し、以降そのキャラのステータスが変化しても過去メッセージの表示だけは変わらない——チャットログがそのまま状態変化の記録になる
- Worldの編集フォーム・設定画面のそれぞれに3×3のチェックボックスグリッド（`StatusDisplayGrid`）を追加

**キャラ／World／部屋テンプレートの画像込みエクスポート・インポート**
- キャラシート＋立ち絵/表情画像一式、World（代表画像込み）、部屋テンプレート（背景画像込み）を、それぞれzipバンドル（manifest.json＋images/フォルダ）としてエクスポート/インポートできる機能を追加。新規依存`adm-zip`
- Worldエクスポートは所属する部屋テンプレートを含めるか、含める場合は登場キャラクターまで含めるかをエクスポート時にチェックボックスで選択可能。部屋テンプレート単体のエクスポートも登場キャラクターを含めるか選択可能
- インポート時、関係性軸・表情タイプ・画像スタイルプリセット・設備機材の参照はいずれも名前ベースで解決（IDはインストールごとに異なるため）。部屋テンプレートの登場キャラ・部屋間接続の参照解決は、インポートバッチ内の全キャラ・全部屋テンプレートの作成が完了した後にまとめて行う
- 部屋テンプレート単体（Worldデータを含まないバンドル）のインポートでは、対象Worldをインポート時に指定する（`target_world_id`）
- キャラクター／World／部屋テンプレートの各一覧画面にエクスポートボタンとインポート用ファイルピッカーを追加

### v0.0.5

**チャット応答内での動的アイテム生成**
- LLM出力の`[ITEM_GRANT: アイテム名|カテゴリ名]: 説明文`タグをパースし、現在の部屋のWorld固有枠にfind-or-createでアイテムを登録・付与。テキストのみ（画像生成は見送り）

**キャラクター応答の長さ調整**
- World設定に「応答の最大長さ（トークン数）」を追加（chat_reply専用、既定512）

**関係性変化のアナウンス通知**
- World設定のトグルで、関係性変化をSCENE_CHANGEと同様のインライン通知としてチャット画面に表示。あわせてSCENE_CHANGE通知が消えない不具合も修正（両方とも6秒で自動消去）

**アイテムカテゴリ＋行動コマンドの自由拡張**
- アイテムに共通＋World固有の2段構成のカテゴリを新設。動的アイテム生成もこのカテゴリ一覧からLLMに選ばせる
- 固定の「使う」アイコン1つだった仕組みを撤廃し、`item_use`種別の行動コマンドをWorldごとに自由に何個でも定義可能に。各コマンドで「消費型として扱うか」「対象へ所有権を移すか（渡す系）」を個別指定。消費判定は常にコマンド単位の設定が優先（カテゴリ側は分類の目安のみ）
- アイテム系コマンドを開いた時点で、入力欄に`@名前`のメンションが既にあれば対象を自動選択

### v0.0.4

**部屋＝「場所」の再検討（部屋間移動）**
- 部屋テンプレートに`is_place`フラグ＋片方向の「つながり」（`room_connections`：移動先・ラベル・移動コスト）
- World単位の「時間帯ごとの移動サブカウント上限」。移動は時間を1コマ強制せず、コストを積算して上限到達時のみ時間帯が進む
- 「同行させる」フラグでキャラを移動先セッションに引き継ぎ可能
- 終了済みセッションを閲覧できるセッション履歴一覧・閲覧専用ログビューアを新設

**チャット画面**
- フルスクリーンレイアウト化、アバター表示の大型化（56px角丸）
- LLM応答をスクリプト行単位で逐次パース・配信（一括表示から段階表示へ）
- 未知EMOTIONタグのフォールバック方式改善（セリフは活かし表情のみ既定に）
- 空欄送信＝「続きを生成」の明示的操作として扱う仕様追加

**アイテム・行動コマンド・メンション機能**
- アイテムの構造化管理（World共通＋World固有）、プレイスルー単位の所持品
- アイコン付きクイック操作バー、自由入力パネル
- Teams風@メンションによる対象キャラ明示指定
- イベント条件`has_item`、アクション`grant_item`/`remove_item`

**イベント機能強化**
- キーワード条件の「+追加」ボタン（モバイルのEnter問題対応）
- 成功／失敗の結果分岐（`phase`条件、`outcome`アクション、`llm_judge`条件）
- イベント連鎖（前提イベント参照＋結果条件）
- イベント定義のJSONエクスポート/インポート（キャラ・部屋・前提イベント参照の名前ベース解決）

**KoboldCpp起動設定**
- `--sdquant`選択、モデルパス直接指定
- 画像生成用LoRAのパス・強度指定、LoRA×sdquant併用不可の検出とエラー化

**画像生成設定**
- 種別ごとのネガティブプロンプト設定
- 参照アンカー幅の完全動的化（参照画像の実サイズ・アスペクト比ベース）

**キャラクター属性キー**
- キャラクター／World／部屋テンプレートへの自由記述「属性キー」追加
- `character_join`の属性キー一致による自動選出（`tag_match`）、初期参加キャラ一覧での★ハイライト
- 属性不一致キャラの入室拒否ガード

### v0.0.3
- **主人公（あなた）機能**：World単位の既定設定＋ルートごとの上書き、`character`/`narrator`（神視点）モード、主人公の自己発言をシステムプロンプト＋パース後コードの二重で抑止
- **画像生成設定の拡張**：画像種別ごとに生成方式（アンカーi2i／プロンプトのみ）・プロンプトテンプレート・キャンバスサイズ・SDパラメータ・サンプラーを個別設定、種別ごとのテスト生成機能
- **KoboldCpp起動導線**：`start-koboldcpp.bat`の自動検出起動、アプリ内「KoboldCppを起動」ボタン（デタッチ起動）

### v0.0.2
- 配布用リリースパッケージ作成ツール
- 開発プロキシのポート固定不具合を修正（複数インスタンス同時起動時の画像取得ミス解消）

### v0.0.1
- KoboldCpp連携（テキスト／画像生成）、複数キャラクターのスクリプト形式レスポンス解析
- 参照アンカー方式による立ち絵・シーン・イベント画像生成パイプライン
- イベントルールエンジン（条件／アクション／エディタUI）
- 設定画面（接続状況・LAN情報・画像スタイルプリセット・出力形式・テスト生成）
- レスポンシブレイアウト、デモ用世界観コンテンツ

---

## 未リリース（作業中・バージョン未確定）

現在進行中の作業なし。引き続き、遊びながらのデバッグとプリセットコンテンツ（デモWorld・キャラ等）の拡充を優先する方針。

---

## ロードマップ（未着手・設計待ち）

### コンテンツパイプライン
- **TTS対応** — KoboldCpp自体は`--ttsmodel`等のCLIフラグを既に持っている（未使用）。モデル配置・API連携・再生UIが必要
- **SillyTavernカード形式インポート＋和訳** — `characterSheetFormat.js`の`parse()`を新フォーマット対応として拡張、英語データの自動和訳ステップを追加
- **Qwen-Image-Edit対応** — KoboldCpp本体はv1.101.1以降で公式サポート済み（開発者本人が動作確認済み）。ただし本プロジェクトの手元検証では、非公式「AIO」モデル一式（VAE・テキストエンコーダ未統合、mmprojの変換方式がKoboldCppのローダーと非互換）は不可と判明。公式リンク先の構成一式（QuantStack + mradermacher、計約16.6GB）は未検証のまま
